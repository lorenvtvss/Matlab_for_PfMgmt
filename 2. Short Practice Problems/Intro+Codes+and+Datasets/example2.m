function f = example2(x) 	% the input data is x, and
						    % the function returns the value of f
y = 0;						% define working variable y 
                            % for the computations
y = x + x^2 + x^3 + x^4;	% perform the computations
f = y;						% return the result
